[project]
name = "mon-parcours-sante"
version = "0.1.0"
description = "Mon Parcours Sante ADK Agent"
requires-python = ">=3.10"
dependencies = [
    "google-adk>=1.25.0"
]
[tool.setuptools]
packages = ["mon_parcours_sante"]